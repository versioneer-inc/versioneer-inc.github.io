# versioneer-inc.github.io

Static GitHub Pages site with `uv` tooling.

## Quick start

```bash
uv sync --dev
uv run preview
```

Open <http://127.0.0.1:8000>.

## Formatting

```bash
uv run pre-commit install
uv run pre-commit run --all-files
```
